env: "local" #local, dev, prod etc...
storage_path: "./storage/storage.db"
http_server:
  address: "localhost:8082"
  timeout: 4s # Время на чтение запроса и такое время на отправку ответа
  idle_timeout: 60s # Время жизни соединения с клиентом
  user: "myuser"
  password: "mypass"